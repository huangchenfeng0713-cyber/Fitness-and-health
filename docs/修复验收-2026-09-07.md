# 修复与验收记录（2026-09-08）

## 范围与分支

审查基准：`huangchenfeng0713-cyber/Fitness-and-health`，`main`，`9021b707cb54fc81f2a88035efe3f5b874ca180d`。

开始时当前任务目录为空。先检查本机现有仓库 `fitness-and-health-4-11-g`：HEAD 与基准相同，无已跟踪文件改动，仅有未跟踪 artifacts。以本机仓库建立独立副本后在当前目录修改，原仓库及其 artifacts 未动。本轮继续时重新检查未提交差异，在已有修复上追加，没有按报告旧行号覆盖。

修复验收阶段未提交、推送或部署；2026-09-08 用户随后明确授权合并部署，发布版本为 3.15.0。浏览器使用独立临时上下文及合成样本，没有连接账号或导入真实健康数据。未修改 Supabase 表、部署函数或真实快捷指令。

“实现完成”表示当前挂载代码与本地回归已覆盖；真机、外部来源和真实同步的未验证项单列，不把它们算成通过。

## 逐项结果

| 项 | 状态 | 当前行为与验证证据 |
|---|---|---|
| F01 | 实现完成 | `energy-observation.js` 统一空值/零、独立字段时钟、未来/过期/错日/可疑/未对齐状态。圆环、建议与配对趋势复用；固定样本验证异常不进收支。 |
| F02 | 实现完成；真机待验 | 完整日需覆盖与字段截止证据，部分日及旧记录覆盖未知不入完整日基线；至少 3 个完整配对日。23/25 小时契约、跨日及不足样本回归通过。 |
| F03 | 实现完成 | 每日计划来源与今天设备观测分离；当前提示改读统一观测结果，不再用目标的旧 `activeCapped` 决定是否同步。同步增加活动不改变已保存计划。 |
| F04 | 实现完成 | 100 kcal 或三餐各有一条不代表一天完成；日内正收支不推定全天摄入够；21 点未吃正餐继续保留。今天建议不再按时钟自动“结算”。 |
| F05 | 实现完成 | 历史/未来统一入口返回回顾/预填状态，不挂载当前饮食推荐或即时行动。浏览器两种日期均检查通过。 |
| F06 | 实现完成 | 缺失/非法/范围外生日分开，无效档案返回不可生成，页面和修复入口可用；不使用默认人的目标。BMI 原值分类，低 BMI 减脂冲突停止自动限制；失效的默认档案演示按钮移除。 |
| F07 | 实现完成 | 体脂 2%–70% 边界一致；蛋白按体重，移除 BMI 阈值跳变并标分母。Katch 需明确选择和近期有效体脂，不无条件宣称更准确。 |
| F08 | 实现完成 | 草稿与保存同完整求解器；BMI/瘦体重/BMR/TDEE 也实时刷新。方向冲突返回不可生成，实际预算、输入意愿、限制原因分开。浏览器修改身高/目标/速率后预览与保存一致。 |
| F09 | 实现完成 | 总糖、游离糖、未知值与显式 0 分开；旧自定义记录只读标待确认、保留旧数值，可逐条核对。未知营养在记录和合并行中不会显示成完整零值。 |
| F10 | 实现完成 | 总碳水/可利用碳水按显式口径归一化；每 100 g/ml 保留。写入、恢复、合并和汇总验证有限非负值，布尔值不冒充数字；异常带定位及修复入口。包装热量不被 4/4/9 回写。 |
| F11 | 实现完成 | 周图和摘要共用完整能量配对规则；缺活动不算 0，明确配对天数。各营养素均值使用各自有效分母，部分已知日不冒充完整营养记录。 |
| F12 | 实现完成 | 体重绘图、拟合、解读同窗口；7 日窗口不足 7 天跨度不假造周斜率。目标传明确引用日期，保存版本/生效日；无旧版本明确当前设置对照，跨版本不画伪统一目标线。 |
| F13 | 实现完成 | 去掉短样本推整周运动达标与主区每周脂肪当量，蛋白 100% 达标；心率 50–59 分支和精度统一，保留症状与就医说明。 |
| F14 | 实现完成 | 低值不能触发千倍修复，必须有解析器/原单位/原值证据；预览、原值与修复值快照、写后验证及撤销。浏览器验证修复、撤销保留其他字段，并拒绝覆盖修复后新录入的能量值。 |
| F15 | 实现完成；来源部分待核实 | WHO 钠与糖参考、National Academies 成人脂肪范围及碳水参考已核对；钠 AI 年龄表未成功核实，已撤下。5% 糖参考不当疾病危险线。其余既有文献核实状态见算法依据。 |
| F16 | 实现完成 | 共同主练肌群用交集，主练/协同语义一致，相似仅说明模式。周上下文决定覆盖建议，单日不强求平均。负荷量说明已录重量×次数，不等于净做功/热量。 |
| F17 | 实现完成；真机待验 | 区间按实际边界协调来源，保留互补时间段；固定样本覆盖重叠、同 5 分钟内不重叠、跨午夜及 workout-only。快捷指令说明已改为来源核对，未声称真机验证。 |
| F18 | 实现完成；真机待验 | 未识别单位拒绝；Cal/cal 区分，百分比依格式契约。日期本身不伪造截止时刻，ActivitySummary 替换值时同步替换来源时钟；旧字段游标兼容保留。 |
| F19 | 代码与依据更新完成；来源部分待核实 | 重写算法依据，区分已核实文献、既有出处与产品护栏。未挂载 `healthInsights`/`healthSummary` 标记 deprecated，维持兼容测试，明确不得挂回当前推荐入口。 |

## 文案与提示生命周期

按报告 A/B/C/D 分类审查了今日、饮食、数据、健身、身体目标、计算偏好、同步、备份及错误/空状态。

- A：保留单位、份量、每 100 g/ml、日期、有效期、备份/恢复/删除范围、来源区分与必要风险。历史体重日期直接可见。
- B：缺字段、过期、未来、可疑、计划冲突、未知营养、未核实旧记录只在条件出现时提示；错误能定位记录或进入表单修复。
- C：详细公式、估算局限、训练负荷统计、导入来源/异常定位放可访问说明/折叠详情。诊断展开状态跨重绘保留。
- D：删除固定进食节奏设置、重复可选加餐说明、失效默认档案演示入口、历史的当前行动。预算和指标按内容折行，不再强制三栏或等宽。

具体生命周期验证：空档案→可修复页面；草稿修改→实时预览；保存→从当天生效；缺观测→不显示伪收支；历史/未来→无实时推荐；旧营养→待确认入口；单位修复→可撤销；后续新改动→撤销拒绝覆盖。数值点击浮层保留，四角定位、Esc 关闭通过。

## 本地验收证据

环境：Windows，Node v24.19.0，独立 Playwright + Microsoft Edge，`http://127.0.0.1:8080/`。Browser 插件当前不可用；此前运行时返回 No browser is available。使用已有 Playwright/Edge，没有安装项目依赖。

| 验收 | 结果 |
|---|---|
| 全部现有单元测试与必要回归 | `node --test test/*.test.js`：685/685 通过，无跳过。等同 package.json 的 test 命令；环境未提供 npm 命令。 |
| 现有浏览器冒烟 | `scripts/smoke.mjs`：32/32 通过，含四页、读写刷新、搜索焦点、图表横扫、弹层手势、设置分组和离线外壳。 |
| 发布前补齐 CI 定向冒烟 | `pwa-upgrade`：旧外壳升级 3.15.0 通过；`diet-today` 33/33、`today-metric-values` 79/79、`intake-training` 19/19、`diet-interactions` 36/36、`meal-advice-food` 13/13。修正旧测试对默认档案、全天摄入判断、未知营养字段及已删除设置的假设，保留读写与浮层交互检查。 |
| 页面响应式 | 320/375/390/430/1280 px × 正常/200% 文字 × 四个主页面：40 组，无可见越界、无运行异常。 |
| 健康字段组合 | 8 个字段的 256 种有/无组合 × 5 宽度 × 2 字号：2560 组，无指标数值/标签溢出；包含 5 位数字。 |
| 专项交互 | 21/21：空档案、草稿实时更新、保存一致、无效生日、320 px 设置文字放大、四角数值浮层与 Esc、历史/未来隔离、长名称旧营养修复入口、能量修复与撤销冲突。 |
| 静态检查 | `git diff --check` 通过；模块引用/缓存资产由既有测试验证。 |

200% 检查把每个元素的实际计算字号加倍，保留原视口宽度；不是将截图放大，也没有以页面缩放掩盖文字裁切。已修复首次截图发现的圆环图例越界、营养省略号、固定输入高度裁切和顶栏日期裁切。

证据保存于本机 `D:/Codex-task-temp/fitness-health-review/`：`final-unit-tests.log`、`smoke.log`、`browser/acceptance.json`、`browser/health-combinations.json`、`browser/interactions.json` 及对应截图。独立 QA 脚本亦在该目录，未混入应用源码。

## 仍需外部或真机验证

1. iPhone / Apple Watch 快捷指令：来源筛选是否可靠、空查询与显式 0 的实际输出、完整自然日查询区间、上传游标与观测截止是否一致。没有调用真实设备或上传数据。
2. iOS Safari/PWA：系统文字放大、软键盘、旋转、刘海/安全区、系统日期控件及触摸浮层。桌面模拟不替代这些验收。
3. 跨时区与夏令时真实导出：共享契约支持 23/25 小时，固定样本验证通过；XML 没有充分时区/覆盖证据时保守排除基线，不冒充已完成真实 DST 导入验证。
4. 中国 DRIs 年龄钠原表、Mifflin 原文全文、Katch 教材原页及食物库逐条原始标签，没有全部重新取得并验证。未核实的年龄钠逻辑已禁用，既有食物来源不声明为本次全面复核。用户自定义未知字段和历史迁移不会猜成已知值。
5. 未执行真实账号跨设备同步、真实备份覆盖或真实健康数据修复。兼容验证使用固定样本和本地隔离数据库。网页发布结果以本次合并后的 GitHub Actions / Pages 记录为准；发布不包含后端或真实数据操作。
