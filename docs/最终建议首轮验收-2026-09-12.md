# 最终建议首轮验收（2026-09-12）

工作分支：`codex/final-recommendations-20260912`。基准为实际拉取的 `origin/main`：`f5ef428acd151b5e3d51251dedb5b7e64dee756c`（v3.18.0）。开始时工作区干净，没有覆盖用户未提交改动。新代码版本 3.19.0。9 月 12 日完成首轮时未提交或发布；9 月 13 日用户明确要求完成后合并，本记录补充后续修复，发布状态以对应 PR 为准。

按附件第 9 节实施批次 0/1，确定批次 2 字段语义；未将全文的未来规划一次性启用。

## 完成情况

| 范围 | 结果与证据 |
| --- | --- |
| 训练备份遗漏 | 导出和账号快照包含 training。真实 IndexedDB 清空后恢复逐组数据、null、0 及原始可选字段，均保持一致。 |
| 旧备份 | 未包含 training 键时文件恢复保留当前训练；明确空数组仍能删除；null/错误类型拒绝恢复。预览展示训练天数或保留范围，取消恢复不改数据。 |
| 账号同步 | 模拟上传→另一设备下载训练不丢；同账号旧快照保留并补传训练；相同 revision 的漏表也补传；明确空数组同步删除。未归属设备明确选择云端不会隐式携带训练。保留原 revision/epoch/并发写保护，无数据库表或远端协议变更。 |
| 每日计划依据 | 新目标快照保存 planBasis 窗口、完整设备日数量和来源，读取时返回生效日。旧目标无证据时标明未知。实际 TDEE、摄入目标、设备基线分开说明；不回填旧目标。 |
| 七日同样本统计 | 已记录日均与配对日均明确区分；配对摄入、设备消耗、累计差值使用同一日期交集。帮助可查看日期与样本数。用“额外饮食日、缺活动、部分设备日”固定样本验证。 |
| 热量说明 | 保留现有圆环、数值浮层和必要风险说明。TEF=0 只解释为未单独加算；不宣称已证明 Apple 包含/排除，也不加固定 10%。活动系数说明涵盖工作、通勤、家务和训练。 |
| 训练记组 | 面板显示最近一次日期与逐组次数/重量，摘要补次数。首组采用上次第一组，后续复制上一组；均先停在页面草稿，确认才写入，取消不计数。刷新/换日/账号边界清理草稿。 |
| 自重与未知 | 自重动作可只记次数，不换算等效公斤。新空白保持 null，显式 0 原值展示；历史 0 可能原本未填，不反推为自重或附加负重。重量口径的新增枚举留在批次 2，不凭动作名迁移旧数据。 |
| 快捷体重 | 今日卡和数据页共用入口；复用健康日表、saveHealthDay 和 manual 字段来源。空输入不写入，保存其它字段保持原样，已有不同体重须确认替换。已保存计划继续保持。 |
| 移动布局 | 320/390/430px 检查；320px、字号 token 放大到 200% 验证新草稿、体重表单、七日表。修复表格挤压与标题重叠，维持按内容分配空间。 |
| 离线外壳 | 新模块加入 SHELL，版本号/README/反馈模块/缓存键同步到 3.19.0；PWA 升级回归通过。 |
| 后续契约 | `记录契约-2026-09-12.md` 明确日级饮食完整度、组完成状态、RIR、组类型、计时和重量口径；这些持久化字段与周期反馈尚未启用。 |

## 检查结果

- 基线：702 项单元测试通过。9 月 12 日 710/710；9 月 13 日零基线修复后：`node --test test/*.test.js`，**715/715 通过**。
- Chromium：现有 9 套浏览器脚本与新增 `final-recommendations-smoke.mjs` 均通过。依次为 smoke、pwa-upgrade、diet-today、today-metric-values、intake-training、training-design、diet-interactions、meal-advice-food、food-muscle 及 final-recommendations。
- 新增脚本 **32 项检查通过**；WebKit 同脚本 **32 项检查通过**。包含真实 IndexedDB、文件恢复预览取消、草稿确认、手动体重来源、目标生效边界与文字放大。
- 9 月 13 日修复后重新执行 Chromium 新增脚本，32 项通过，无运行时错误。
- `git diff --check` 通过；新增浏览器脚本已接入 CI。未新增纯措辞镜像测试；删去两条已与实际恢复契约冲突的旧文案断言，由真实恢复交互接替。
- 所有浏览器数据都是独立临时上下文中的合成记录，外部请求被拦截。未读取或修改真实健康数据、真实账号快照。
- 本地日志及截图：`D:/Codex-task-temp/fitness-final-20260912/`。Windows 的 Chromium/WebKit 模拟不能当作 iPhone Safari/PWA 真机验收。

## 修改文件

- 存储与计划：`js/lib/db.js`、`js/lib/cloud-sync.js`、`js/lib/store.js`。
- 计算/记录描述：`js/core/weekly-summary.js`、`js/core/training.js`；`js/core/nutrition.js` 修正食物热效应注释，并于 9 月 13 日修复零活动基线及非法类型分支，未改计算常量。
- 页面与共用入口：`js/views/dashboard.js`、`js/views/training.js`、`js/views/settings.js`；`js/views/cards/{data-manager,health-metrics,profile,weekly-summary,weight-entry}.js`（weight-entry 为新增）。
- 视觉与离线版本：`css/app.css`、`sw.js`、`package.json`、`js/core/feedback.js`、`README.md`。
- 验证：`test/{account,nutrition,review-regressions,training,weekly-summary,ui}.test.js`；`scripts/{intake-training,training-design,final-recommendations}-smoke.mjs`；`.github/workflows/ci.yml`。
- 文档：`docs/算法依据.md`、`docs/CLOUD_SYNC.md`、本验收记录、`docs/记录契约-2026-09-12.md`。

## 尚未解决与后续验收

1. **零活动基线问题已于 9 月 13 日修复。** `dynamicTDEE({bmr:1500, baselineResting:1500, baselineActive:0, observationFraction:1, fallbackTDEE:2250})` 现在返回活动 0、TDEE 1500，来源 `device-baseline`。新增回归覆盖明确零、缺失与非法类型、有效今天观测、异常观测原值保留，以及完整历史进入计划求解/缺失历史退出基线；旧目标快照不被改写。
2. 旧快照本来未包含的训练不能凭空恢复；仍运行旧客户端时可能再次漏表，发布后各设备需要升级。真实 Supabase 双设备登录、离线冲突和安全退出需用隔离测试账号验收；本轮只验证客户端模拟流程。
3. iPhone Safari/PWA：软键盘与原生日期控件、VoiceOver、系统大字、屏幕安全区、弹层材质/透底与触控仍待真机。WebKit Windows 截图的背景模糊与真机不等价；未声称已通过真机视觉验收。Apple Health 快捷指令行为仍未真机验证，也未修改其实现。
4. 旧训练的 0 与历史未知无法自动区分；单手/合计、附加/辅助仍未有明确字段。日级饮食完整度、RIR、工作组统计、计时、周期反馈、历史驱动推荐及卡尔曼/RTS均按后续批次推进。

本轮不生成个人热量处方，不改真实健康数据，不部署自动校正。附带文档中的参考链接不作为新增科学主张已核实的证明。
