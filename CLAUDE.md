# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## 命令

```bash
npm test                                              # 全部测试（node --test）
node --test test/nutrition.test.js                    # 单个文件
node --test --test-name-pattern='Katch' test/*.test.js  # 按用例名筛选
npm run serve                                         # python3 -m http.server 8080

# 浏览器冒烟：起动、四个栏目、IndexedDB 落库、Service Worker 接管
# CI 会自己跑；本地要跑得先起服务器，playwright 用 PLAYWRIGHT_PATH 指过去
node scripts/smoke.mjs http://127.0.0.1:8080
```

`.github/workflows/ci.yml` 在每个 PR 上跑单元测试 + 浏览器冒烟。
**浏览器那一层不是可有可无的**：启动失败吞掉自救按钮、身体信息不合格白屏、
推荐份量漏出浮点数，这三个都是几百项单元测试全绿的情况下漏过去的。

**没有构建、没有依赖、没有 lint 配置。** `package.json` 里没有 `dependencies`，测试直接跑源码。
不要引入 npm 包、打包器或框架 —— 浏览器直接加载 `js/` 下的 ES 模块。

页面必须走 http(s)（ES 模块 + Module Worker），双击 `index.html` 打不开。

## 架构

### 三层纯度边界

| 层 | 能碰什么 | 谁能测 |
| --- | --- | --- |
| `js/core/*`、`js/data/*` | 纯函数，无 DOM、无浏览器 API | 测试只 import 这两处 |
| `js/lib/*` | IndexedDB / Worker / DOM 工具 / SVG | 否 |
| `js/views/*` | 只做 DOM 渲染 | 否 |

新增的计算逻辑一律放 `core/`，否则写不了测试。视图里不要出现营养计算。
「该显示哪几个动作」「这条曲线该怎么解读」这类挑选与措辞逻辑也算计算，
同样放 `core/`（见 `training.js`、`trend-reading.js`）。

### 栏目与卡片

底部四栏：**今日 / 饮食 / 数据 / 健身**，设置在右上角的侧边抽屉里。

**只有「今日」和「饮食」跟顶栏那个日期走**（`TABS` 里的 `dated: true`）。
数据页、趋势图和健身页一律钉死真正的今天 —— 跟着翻的话，「今日健康数据」
这个标题会变成假的，「近 7 日」在不同页面上也会指不同的七天。
顶栏的措辞在 `core/day.js` 的 `dayHeading`：今天 / 昨天给「词 + 日期」，
更远的日期标题就是日期本身（`08月27日`，跨年补年份），副标题只留「回今天 ↩」。
**同一个日期不许上下各印一遍**——原先大标题写「昨天」、副标题又写「08-28 · 回今天」。

各页只回答一个问题，别把「我怎么样」和「我该做什么」混在一起：

- **今日** —— 我今天怎么样。主卡 + 今日提示，两张。
  主卡：热量圆环 + 收支、蛋白一条、碳水脂肪合用一条，下面四个方框
  （纤维、钠、游离糖、饮水）。
  **热量圆环：环形跑道，不是目标进度条**（`core/energy-ring.js`）。
  绿是摄入，黄是消耗，同一把当天锁定的尺子（`round(预计日消耗/100)*100`）。
  圈心只说「摄入领先 / 消耗领先 / 接近平衡」，不写「目标」。
  摄入越过消耗的那段外圈改深绿；消耗套圈会把扫过的绿弧擦回灰轨。
  不要把整圈解释成摄入目标，也不要在环下再放三组数字或四段图例。
  **主卡最上面那一段只说热量**（`judgeStatus`），计划速率的代价也算热量的事
  （`rateNote`，紧跟在它下面）。蛋白缺口、钠超标这些一律归下面的「今日提示」：
  它们在那儿本来就各有一条，同一件事在一屏里说两遍，还会把热量的结论顶掉——
  实测钠只超 11%，首段就变成了「钠摄入超标 11%」，而那天还差 900 kcal 没吃。
  目标依据和 Apple 能量的时效收在主卡右上角的 `infoTip` 里；
  但「身体信息不合格 / 演示数据 / 数据过期」这几条不许收进去，
  它们说的是「你现在看到的数字不对」，藏起来就没人会发现（`energyFreshness`）。
  只读的「今日记录」已删：饮食页那张是可编辑的超集，看到了却改不了反而要再翻一页。
  多日趋势（日均摄入、蛋白达标率、体重斜率）也不在这里，归数据页的图。
  **今日提示是三段式**：当前情况（title）/ 判断依据（basis）/ 可执行建议（action），
  按 `INSIGHT_PRIORITY` 排序 —— 1 数据问题（漏记、没同步、数值不可信）、
  2 热量和蛋白、3 明确门槛（钠、糖、纤维）、4 碳水脂肪结构、5 活动睡眠饮水。
  数据问题排最前面是因为下面每一条的可信度都取决于它：漏记半天的人，
  「热量还差 900」是假的。默认三条，其余折叠。
  判断依据不在每一行各放一个信息按钮；只在卡片右上角留一个入口，并按当前实际显示的
  `list` 动态汇总。展开更多提示时，说明层也必须同步增加对应依据。
  **没有依据的话填不进第二段**——「吃得慢一些」当年就是这么混进来的，
  程序根本没有进餐时长数据。
- **饮食** —— 我现在该吃什么。喝水（计次）、搜索记账、当天记录、当前饮食推荐。
  顶部那条「热量余量 / 蛋白还差 / 下一餐预算」已删：份量面板本来就写着
  「记下这一笔会推进到哪」，那才是做决定的时刻；而「今天整体怎么样」是今日页的问题。
  摆在这儿只是把同一批数字提前念一遍，还把搜索框顶到了首屏之外。
  搜索框右侧有个清除键，有内容才出现（`type="search"` 在 iOS Safari 上没有原生小叉，
  而这个应用主要就在 iPhone 上用）。搜索认拼音首字母（`core/pinyin.js`）：
  别名里本来就写着全拼，把它切成音节再取首字母，`tznnf` 也能找到「脱脂牛奶粉」；
  切不干净的（英文单词、缩写）返回空串回退到子串匹配，不去猜。食物和动作共用这一个匹配器。
  **从今日提示跳进来的那一列食物要给得出出口**：标题那行前面是一枚可关闭的胶囊
  （`.focus-chip`，标签 + ×），点一下回到推荐。原先唯一的退出方式是往搜索框里打字
  —— 打字会顺手清掉 focus，可谁也猜不到这一点，人就困在一列鳕鱼牛筋里出不去了。
  搜索框下只保留按真实使用记录生成的「历史」，不再铺一整排分类标签：
  知道食物名就搜索，不知道吃什么由下方推荐回答。历史收起时按按钮真实高度
  精确保留两行，禁止用固定像素高度截出半行；每条记录可用垃圾桶图标单独删除。
  **＋ 和点行本身都开份量面板**：加进备选（`ui.basket`）之前先看一眼这次记多少。
  ＋ 曾经是「直接进备选、用记住的份量、不开弹层」，按操作次数算那样更少，
  但记住的份量只是上一次的克数，同一样东西这顿吃多吃少它不知道，
  而克数是乘数，差一倍热量就差一倍。已经在备选里的那一行 ＋ 变成 ✓，点一下移出去。
  备选只在页面内存里，按备选条上那一下**勾**才落库；`addEntry` 在这一页只该出现一次
  （`test/ui.test.js` 会数）。餐次是**整批**的选择，写在备选条的副行里，
  因为确认键上只有一个勾，落到哪一餐得在别处说清楚。
  **「饮食记录」那张卡默认只读**，按「编辑」才给出克数输入框、餐次下拉、删除和「清空这一天」：
  它大部分时候是拿来核对「今天吃了什么」的，每行挂个红叉滑列表时容易蹭到，
  而删掉一条没有撤销。上面那张搜索卡因此改叫「添加食物」，两张卡不能重名。
  五个餐次标题各带一个**单色线条图标**（`MEAL_ICON`）：五个标题都只有两个汉字，
  滑到一半分不出看的是哪一餐。不用彩色 emoji —— 那在三个平台上是三套画风。
  选中食物后份量面板从底部升起（`.sheet`），不再挂在搜索结果下面——
  原先选完要往下滚过整列结果才看得见。记录按钮 sticky 在弹层底部。
  **推荐行的 ＋ 和搜索结果的 ＋ 走同一条路：先开份量面板。**
  原先它按推荐的克数直接落库 —— 可那个克数是「按剩余预算算出来的一份」，
  不是这个人自己的份量，而克数是乘数。同一个加号在两处做两件事本身也说不通。
  `addEntry` 因此在饮食页仍然只出现一次。
  **食物旁边不写分类**（肉禽 / 菜肴外卖…）：挑食物时它不参与判断，
  一列结果每行挂一个灰块，比食物名还抢眼。弱标签（`.ui-weak-tag`）也不撑
  最小高度、内边距贴着字 —— 原先它比它注解的食物名还高一截，
  注释反倒比主文字显眼。
  **喝水那条横幅上，次数一直写着**（原先记完那几秒会换成「已记录一次饮水」，
  数字消失，而那正是人最想看到它加一的时刻）。点完水滴会流到数字上，
  位移距离要挂进 DOM 之后量一次写进 `--flow-x`，量不到就不放动画 ——
  猜一个距离的话水滴会停在半路或者飞出卡片。动画的时间窗（`FLOW_WINDOW_MS`）
  不能用布尔量代替：一次点击会引发两次渲染（落库的 emit 一次、await 之后一次），
  布尔量会被第一次取走，而那个节点马上被第二次换掉。
  推荐原先长在今日页，但真要照着做的时候人已经在这一页了。
  「现在别碰」整块已删（连 `buildAvoidList` 一起）：说的是「别做什么」，
  和这一页要回答的「该吃什么」是反过来的，而且一次列五条几乎每天都一样。
  **份量记的是这个人自己的份量**（`store.rememberedPortion`）：某样食物上次记了多少克，
  下次选中就默认多少。同一个人的一碗饭每次都是同一碗，库里那份「1 碗 = 150g」
  只是没有更好答案时的兜底。用的就是库里现成份量时不记（没什么可记的），
  存在 `settings.portionMemory`，上限 200 条。
  **糖度和餐次跟克数一起记**：一条记忆是 `{ grams, sugarLevel, meal }`，
  老数据是个裸克数，读的时候当成 `{ grams }`，不迁移。
  同一杯奶茶总是三分糖、同一样早餐总是记到早餐，只记克数那两个每次都要重选。
  餐次只在这次还没手动选过、且备选是空的时候才让位 —— 已经选了「记到晚餐」
  不能被下一样食物的记忆拽回早餐。
  **一条饮食记录必须是自洽的**：名字、克数、单位和七项营养在记账那一刻就存进去了。
  渲染那一行不许依赖 `findFood` 还能不能查到 —— 自定义食物被删、换设备恢复的备份
  没带上它、旧版本留下的 id，都会让它返回 null。原先 null 一路传进
  `isEstimated`（它第一行防了空、第二行直接读 `food.cat`），整个饮食页被错误边界接管，
  而饮食页正是唯一能删掉那条记录的地方，崩了就永远删不掉。
  查不到时标一枚「食物已删除」的中性弱标签，克数、餐次照样能改。
  **这张卡只留一个说明入口**（卡头那个 ⓘ，按当前实际显示的这几条动态汇总：
  估算依据、「记录是记账当时的快照」、真有食物查不到时那句解释）。
  每一行不许再各挂一个 —— 十几条记录就是十几个按钮，说的还多半是同一句话；
  这条规矩在「今日提示」那张卡上已经立过一次。
  **只读态把「同一笔」合并成一行**（`core/diet-log.js` 的 `mergeSameEntries`）：
  同一餐 + 同一食物 + 同一糖度 + 同一单位 + 同一配料才算同一笔，
  克数和营养逐项相加。编辑态一条是一条 —— 删的、改克数的都是某一条具体记录。
  **自定义食物要能改**：分类、能量（可点右边的单位在 kcal / kJ 之间切，
  已填的数字跟着换算）、六项营养、常用份量、是不是饮品。点自定义列表里的名字进入修改，
  保存时沿用同一个 id，否则「改一个数」会变成「多出一条重名食物」而旧记录还指着旧 id。
  删一个还挂着记录的自定义食物要先说有几条，但**不拦** —— 记录本身是自洽的。
- **数据** —— 今天同步了什么 + 在往哪走。今日健康数据 + 近 7 日速览 + 一张趋势卡。
  **「今日健康数据」只说真正的今天**（`core/health-card.js`）：不跟所选日期走，
  但**体重显示截至今天最近一次有效记录**，并在右上角信息按钮里明确测量日期；
  体脂与静息心率仍只显示当天值。缺项、最近一次测量、同步时间和可能原因都收在信息按钮里。
  「已同步 / 未同步」问的是**今天有没有成功同步过**，不是某一项有没有值：
  手表哪天没戴，静息心率就是空的，可那天照样同步成功了。
  体脂和饮水毫升是 `optIn`，只在这台设备真的记到过时才占一格。
  睡眠画 Zzz 不画月亮——月亮和「夜间模式」「勿扰」撞得太狠。
  「近 7 日速览」统计**截至昨天的七个完整日**（`core/weekly-summary.js`）。
  趋势图默认近 7 日，可选 30 / 90 / 全部，同样止于昨天。
  解读收在每张图下面：看着那条曲线读那段话，比先看一堆汇总数字再往下翻要直接；
  统计口径归信息按钮，图下面只写趋势解读、数据质量和实用建议。
- **健身** —— 今天练什么、练了多少。**固定记今天**，历史去「近 7 日训练记录」看。
  页面顺序：今日动作 → 选择动作（部位 / 模式 + 器械档位）→ 动作列表 → 训练建议
  → 近 7 日训练记录。
  **是「2 + 1」，不是三级下钻**：`.picker-controls` 里只放两排 ——
  挑法（身体部位 / 动作模式）和范围（胸 肩臂 背 腿 腹），后者是前者的下一级。
  「全部动作 / 推荐组合」换的是同一批动作的**呈现方式**，不是把范围再切细，
  所以它不进这一组，跟着列表头走（`.picker-list-head`：左边「胸部动作 18 个」、
  右边器械筛选）。做成三排同宽同层等于宣称三者是一条链，试过一版，不对。
  **器械筛选必须看得出能点**：浅底 + 漏斗图标 + 箭头。曾经把它和计数合成
  一行裸文字（`全部 · 18 个动作`），用户当场以为「固定器械 / 徒手」那几档被删了。
  **动作行的「主练 XX」在它等于筛选条件本身时省掉**（`exerciseTags` 的
  `scopeMuscles`）：筛到「胸」时五行全写「主练胸大肌中部」，重复五遍反而
  把真正有区别的那两条挤淡了。两个视图（列表 / 推荐）必须给同样多的标签。
  **动作推荐是「动作列表」那张卡的另一个视图**，不单独占一张卡：
  卡头上那个「推荐」按一下切过去、再按一下切回来（`showRecommend`），
  两边共用同一组开关，换了范围两边一起变。它和列表回答的是同一个问题的两半，
  分成两张卡的话，真正要用的那列动作会被往下推整整一屏。
  推荐原先还只在一个动作都没选时才出现，于是选了第一个之后就再也见不到，
  而那正是最需要「接下来练什么」的时候。
  **推荐认得已经选了什么**（`recommendFor`）：已选的、以及和已选高度重合的都不再推荐，
  否则选完杠铃卧推，第一个推荐还是哑铃卧推。器械档位也管着推荐
  （`EQUIP_FILTERS` 搬进了 core，推荐和列表必须筛在同一个范围里）。
  数量按部位 3–5 个、按推拉腿 4–6 个；全部动作与推荐组合都用 `exerciseTags`
  生成同一套三个短标签（主要动作模式 / 主要肌肉 / 动作类型），并由同一个
  `exerciseMeta` 渲染，不写整句 —— 五条推荐写成五段话，读完比自己翻列表还慢。
  两个视图的动作行还必须共用 `exercise-choice-row`、`exercise-choice-main` 与
  `exercise-choice-action`：不能只让标签相同，文字起点、行内边距和右侧加号仍各画一套。
  两排范围控件使用 36px 视觉高度并在卡内居中；滚出可视区后只显示一行当前筛选摘要，
  点摘要回到原筛选，不另造第二套筛选状态。
  推荐说明使用 `persistentInfoTip`：账号状态或数据同步触发无关重绘时保持展开；
  用户再次点击、点到外部或按 Escape 才关闭。
  已经选了高度重合的一对时直接给替换按钮，**只说最重的那一对**。
  **勾选动作只改 `pending` 和那一行的 DOM，不写库不重绘。**
  原先每点一个都要落库并整页重绘，实测连点三个：页面自己滚了两次，
  同一行的 y 从 813 跳到 201 又跳到 897，列表在手指底下动。
  **没选动作时多选条收薄**（`.select-bar.is-empty`）：「尚未选择动作 / 可连续选择
  多个动作」两行没有信息量，却常驻压着列表（实测 90px → 48px）。
  禁用的主按钮走中性灰，不是把实心绿调淡 —— 调淡看着像个「可以点的次按钮」。
  多选条（`lib/select-bar.js`，和饮食页共用）攒够了一次 `updateSession`；
  健身页把它挂在应用壳的 `#actionbar`，始终固定在底部导航上方；没有选择时保留横幅但禁用提交。
  **重复提示在挑的时候就出**，`clashWith` 要把 `pending` 里还没提交的也算进来 ——
  否则连勾两个卧推变式一声不吭，等提交后才在「训练建议」里读到，那时已经选完了。
  已经选中的行不提示（两行各写一遍「和对方几乎一样」是同一件事说两遍），
  文案要短、用橙不用红（选两个变式是取舍不是错误）。
  勾选后要把整列的 `syncClash()` 都调一遍，但只改那一句，不许 `rerender()`。
  **「今日动作」那张卡只做记录**：动作行、组数、重量、清空。覆盖部位和重复度
  是分析，归「训练建议」，别在同一屏里说两遍。
  **「近 7 日训练记录」一行一个动作**（`recentTrainingRows`），点开才看每组明细。
  重量和次数写成区间（`50–60kg`）：递减组、爬坡加重是真实练法，压成平均数会把它抹掉。
  数字全部来自实际记录，没记就不写。「训练量」这个词以后要留给「重量 × 次数」的容量，
  不能和训练记录混用。
  动作行默认只给一行「主要练哪儿 · 什么模式」；协同肌收在「已选动作建议」那张卡里——
  真要看细节是在排计划的时候，不是在挑的时候。
  挑动作有两种入口，切换即可（`pickMode`）：**身体部位**（`GROUPS`）和
  **动作模式**走 `core/training.js` 的 `SPLITS`（推 / 拉 / 腿 / 核心，按 `pattern` 归类）。
  髋铰链（`hinge`）要看主动肌再分一次：硬拉、山羊挺身这类竖脊肌主导的归「拉」，
  罗马尼亚硬拉、早安式归「腿」。只按 pattern 一刀切会把硬拉分到腿日去。
  **动作的 `group` 必须包含它至少一块主动肌**（`test/training.test.js` 会拦）：
  耸肩曾经列在「肩臂」里，而它唯一的主动肌斜方肌上束属于「背」——
  从肩臂选中它，覆盖点却亮在背，而在背的列表里根本找不到它。
  划船和引体一定要写上肱二头肌、推的动作一定要写上肱三头肌，漏了会让
  重合度算低（弯举和划船看着不重叠），也会让「今天肩臂空着吗」答错。
  **人体部位图删了，别再加回来。** 前后四版都不好看，原因不在画工：
  它在真机上一整块胸大肌只有 19px 宽，那个尺寸画不出能辨认的解剖形状；
  而它占掉 248px 高，把动作列表整个顶到首屏之外（删掉后列表从 y=1014 提到 y=482）。
  正下方那排文字标签做的是同一件选择，说得还更清楚。图唯一多给的信息是
  「今天哪儿练了、哪儿空着」，现在是部位标签上的一个小圆点（`coveredGroupKeys`，
  主动肌 + 协同肌都算）。
  精细的解剖图后来在 `prototypes/muscle-map.html` 里单独画过一版（19 块肌肉、
  每块一条独立路径、能按部位染色）。**结论仍然是不进主程序**：形状画得出来，
  但整图 248px 高时一块胸大肌只有 21px 宽，那个尺寸上任何解剖形状都退化成色块；
  而且几乎每个动作都同时涉及正反两面（卧推的肱三头肌在背面），要画就得画两个人，
  高度再翻一倍。真要用只有一种说得通的场景 —— 点开某个动作时放大了单独看。
  那个目录不参与构建，也不在 `sw.js` 的 SHELL 里。

**设置里「从几个里挑一个」用下拉，不用分段控件**：分段把选项一直摊在屏幕上，
而设置里这些是设完就很少再动的偏好。形式和饮食记录里改餐次那个下拉一致。
（页面内切换「我现在看哪一组」仍然用 `.range-switch`，那是导航不是偏好。）

**设置是一张分组列表**（`views/settings.js`）：身体与目标 / 账号与同步 / 数据管理 /
计算与显示 / 关于与反馈，每行只有图标、标题、当前状态和箭头，点进去才是那张表单。
状态那一列不是装饰 —— 它让「不用点进去也知道现在设成什么了」成立。
只有一种情况例外：账号冲突、待确认归属、锁定必须整屏摆出来，
那说的是「你的数据现在有风险」，收进二级页面等于没提示。

**那个「例外」的判断只有一份**：`lib/account.js` 的 `accountOwnershipUncertain()`，
启动闸门（`app.js` 的 `accountDataLocked`）和设置页共用。两边各写一份的时候，
app.js 收紧过一次而 settings.js 没有，于是首页已经能用了、设置页还空着。

判断里有一条容易写错：**「账号状态还是 loading」只有在这台设备存过账号会话时
才算「说不准」**（`accountSessionMayExist()` 读 localStorage 里的
`sb-<项目>-auth-token`）。从没登录过的人没有上一份记录可闪，却要为一次可选的
云端握手把界面挡住 —— 实测 7.5~8.2 秒，这几秒里首页停在「正在确认账号与本机记录」，
设置页只剩一张账号卡，身体信息、导入备份、反馈全都点不进去。
**云账号是可选增强，不该是看自己数据的前置条件。**
反过来也要成立：localStorage 里有会话时闸门照旧落下，换账号那几秒不能露出上一份数据。

会换页的卡片抽成 `js/views/cards/*`，导出一个挂载函数（`profileCard`、`healthMetricsCard`、
`dataManagerCard`、`trendCharts`、`recommendCard`、`waterCard`），搬家只改一行 import。
`test/ui.test.js` 的断言跟着「页面挂了哪些卡片」走，不跟着文件走。
**视图不许 import 兄弟视图**（`test/module-refs.test.js` 会拦），要复用就抽成卡片。

### store.js 是唯一枢纽，`recompute()` 是整条流水线

顺序有依赖，改动时别打乱：

1. `effectiveProfile` = 用户填的 profile，被 Apple 健康的体重 / 体脂 / 身高覆盖
   （`latestHealthEntry`，取所选日期之前最近一次；来源和日期一并放进 `derived.bodySource`）
2. `computeBaseline(healthDays, dietDaily, day)` → 近 14 天基线
3. `basalMetabolicRate` → `staticTDEE`
4. `dynamicTDEE` —— 仅当 `useAppleEnergy` 且当天有 `activeEnergy`/`restingEnergy` 时才算
5. `dailyTargets(effectiveProfile, dynamic)`
6. `buildAdvice({ targets, intake, entries, profile, health, baseline, now })`
7. 全部塞进 `state.derived`

**视图只读 `state.derived`**，不直接调 nutrition / advisor。所有变更操作
（`addEntry`、`saveProfile`、`mergeHealthDays`、`setDay`…）结尾都是 `recompute(); emit();`。

看历史日期时 `now` 被钉在 `${day}T20:00:00`，否则会按此刻的钟点给出「该吃午饭了」这种建议。

### 渲染模型

`app.js` 的 `subscribe()` 回调整页重跑当前标签的 `render<Tab>(viewRoot)`，另外还有 60 秒定时器
和 `visibilitychange` 也会触发。所以 `render*` 必须幂等、可反复调用。

整页重绘会打断 iOS 输入（收键盘、日期选择器被当场提交），有两道现成的防线，新代码要沿用：

- `app.js` 的 `isEditing()`：焦点在输入控件里时，跳过定时器与可见性触发的重绘。
- `views/diet.js`：外壳只建一次（`buildShell`），之后只刷新 `nodes.*` 里的插槽。**任何带文本输入的视图都照这个写。**

### 导入链路

`views/health.js` 或 `#import=<JSON>` 链接 → `lib/importer.js` → Worker → `core/health.js` 的纯解析函数
→ `store.mergeHealthDays()` → `db.bulkPut(..., { merge: true })`（同日期只覆盖本次带来的字段）。

Worker 协议：`postMessage({ file })` 或 `{ text }`；回 `{ type: 'progress' | 'done' | 'error' }`。

zip 路径是全流式的（中央目录 → `Blob.slice` → `DecompressionStream('deflate-raw')` → `TextDecoderStream`
→ 分块扫 `<Record>`）。`export.xml` 动辄几 GB，**不要引入 DOMParser 或 zip 库**。
能测的解析逻辑都在 `core/health.js`，Worker 只留文件与 zip 的管道。

### IndexedDB

`DB_VERSION = 3`，五个 store：`health`（key 为 `YYYY-MM-DD`）、`diet`（自增 id + `date` 索引）、
`settings`（键值对）、`customFoods`、`training`（key 为 `YYYY-MM-DD`）。
加 store 或索引要同时改 `DB_VERSION` 和 `onupgradeneeded`，并且同步三处：
`IMPORT_LIMITS`（导入体积上限）、`validateImportPayload`（恢复备份时的校验）、
`cloud-sync.js` 里那份 store 名单（漏了换设备就丢数据）。

## 食物库的数据契约

`test/foods.test.js` 会强制以下约束，加食物前先看那个文件：

- `n` = 每 100g 的 `[热量kcal, 蛋白g, 脂肪g, 碳水g, 纤维g, 总糖g, 钠mg]`，七项，全部非负。
- 热量与宏量自洽：`蛋白*4 + 脂肪*9 + (碳水-纤维)*4 + 纤维*2`，误差不超过 `max(25%, 12 kcal)`（`alcohol` 除外）。
- 纤维 ≤ 碳水，糖 ≤ 碳水。
- `s`（常用份量）非空，每份 0 < 克数 ≤ 1000；`cat` 必须在 `CATEGORIES` 里，且该分类要有 `PORTION_TIPS`。
- `cat: 'chain'` 的首个份量必须是品牌标准份（个 / 块 / 只 / 份 / 杯…），40~600g。
- 搜索同分时按数组里的录入顺序 —— 同品牌把主力单品排在配菜前面。
- **糖度是界面选项，不是多条记录。** 加 `tealevel` 标记，营养按全糖录入，`nutrientsFor` 按 `SUGAR_LEVELS`
  五档换算；`sf` 是点“无糖”时仍存在的总糖，`nfs` 是其中不属于 WHO 游离糖的部分。
  乳糖、完整果肉内源糖写入 `nfs`；果汁、果泥即使来自水果仍属游离糖，必须显式写 `nfs: 0`。
- 品牌未公开完整营养表的打 `est`，界面会显示「估算」；茶饮除星巴克 / 瑞幸外一律 `est`。
- `freeSugarFactor` 按 WHO 游离糖定义从总糖中扣除有依据的非游离糖；`fruit`/`veg` 必须是 0，
  但 `snack` 不能按分类一律设为 1（乳品、完整水果制品等可能只有部分糖属于游离糖）。

## 算法的依据

`docs/算法依据.md` 逐条列出了每个进入计算的常数与公式的出处，分三类标注：
**文献**（有公开发表出处，改动前必须给出新依据）、**惯例**（营养实践通行取值）、
**护栏**（纯工程取值，无生理含义）。

改动任何营养或健康计算前先看那份文档，并同步更新它。`test/nutrition.test.js`
里有一组「公式对文献值」的用例锁着 Mifflin-St Jeor、Katch-McArdle、Atwater、
IOM 纤维/AMDR、WHO 钠与游离糖 —— 它们不是回归测试，是防止有人把公式改成拍脑袋的数。

口径红线：

- 界面上凡是基于「惯例」或「护栏」得出的数字，不得说成实测。Apple 能量本身也是设备估算；
  `dynamicTDEE.measured` 只是兼容字段，界面应写「设备累计 / 预计」，不能翻译成「实测」。
- **摄入类结论的分母是「有饮食记录的天数」，不是日历天数。**
  没记录的日子不在样本里，当成 0 kcal 会造出「近 14 天日均赤字 3168 kcal」这种
  并不存在的结论。样本少于 3 天时不下结论（`MIN_POINTS_FOR_CLAIM`）。
- **累计热量收支只算「配对日」**：那一天既有饮食记录，又有设备记录的静息与活动能量。
  少了任何一半都算不出那天的盈亏，配对不足就直说不足，不给数。
  「日均锻炼」取 Apple 健康的锻炼分钟，**不能拿力量训练次数顶替**——
  练了 40 分钟和「训练 1 次」不是一回事，界面上也必须叫不同的名字。
  这几条结论现在只在数据页的图下面出现一次，由 `core/trend-reading.js` 把关；
  `buildInsights` 不再重复一遍，别加回去。
  样本不足时例外：图和解读都不要再解释一长段，只在卡片中央显示一次「数据不足」；
  通用门槛与体重的 4 次 / 跨 7 天门槛收进卡片右上角说明。
- Apple 动态 TDEE 的统一口径是“静息 + 活动”，`tef` 固定为 0；不要再叠加固定 10% TEF。
- **活动能量要过两道合理性判据，缺一不可**：`覆盖分钟数 × 15 kcal/分钟`，
  以及 `max(近 14 天日均 × 4, 日均 + 2500)`。只有第一条时，晚上覆盖分钟数接近 1440，
  上限涨到 21600 —— 日均 600 的人报来 18000（一个月的量）照样放行，
  热量目标被顶到 19717 kcal，而那正是这条护栏要拦的情况。判为不可信时
  不采用该值也不削到上限，改按平时节奏推算，并让界面说出来（`activeCapped`）。
- **指标的性质决定画法，别再合回一根进度条**（`core/metrics.js`）：下限（蛋白、纤维）
  够了变绿；上限（钠、糖）接近变橙、超了变红；区间（热量）落在里面就是对的；
  记录（饮水）**不画条**。区间条整条线性，罩子的位置只由区间本身决定——
  早先那版把区间压在 20%~80%，吃 10g 和吃 30g 的标记位置差不了多少，图在压缩事实。
  **红色只留给真上限**：+0.3 kg/周的增重计划本来就要求每天吃超，把执行计划画成危险色
  是自相矛盾。热量任何情况下都不画红。
- **饮水只数次数，不记毫升，也不画完成条**（`waterCount`）。饮料、汤、粥、水果和
  饭菜里的水分同样被人体吸收，1700 ml 说的只是**直接饮水**；拿它当分母，
  「125 / 1700 ml」会被读成「今天只完成了 7%」，而那个人可能刚喝完两碗汤。
  今日主卡上它和纤维、钠、游离糖并排在方框里，**只有一个数**（`饮水 5 次`）——
  「5 / 8 次」那个分母根本不存在。饮食页那张卡是一行式：左边浅绿状态条 + 右边绿色「＋饮水」，
  点完那几秒里才出现「已记录一次饮水 · 撤销」（`UNDO_WINDOW_MS`），
  撤销一天最多用上一次，常驻就是白占一个控件。
  Apple 健康同步来的 `waterMl` 照存不误，只是不拿来算进度，也只在数据页出现。
- **碳水和脂肪在主卡上合用一条**（`macroSplit` + `splitBar`）。它们分的是同一块热量，
  分开画两条区间时两条能同时「在范围内」而总量差出 796 kcal（2660 kcal 的计划上是 30%），
  各自说自己没问题、合起来对不上账；区间两端还是拿**计划蛋白**解出来的，
  实际蛋白一多一少就整体平移，于是会对着已经吃满热量的人写「碳水低于建议 74g」。
  **画成一根刻度，不是一个靶心**：横轴是碳水占这块热量的百分比，浅色那段是参考区间，
  一个点标出今天落在哪儿，两端分别是偏脂肪和偏碳水，落在区间里写中性的「结构适中」。
  区间两端由**脂肪 AMDR（20%~35%）反解**，不是随手划的一个点，也不是照抄
  碳水 45%~65%。当天吃得还不多时（`MIN_SPLIT_KCAL` / `MIN_SPLIT_SHARE`）只报比例不下结论——
  早饭一碗粥就判「偏碳水」，说的是那一顿不是这一天。
  **结构偏移不画警告色**——怎么分本来就有很宽的合理区间，
  把「今天多吃了米饭」画成橙色是把偏好问题说成健康问题。
  `test/metrics.test.js` 扫 2880 种档案锁住一件事：**照着这个应用自己开的方案吃，
  永远不会被它判成偏。**
- 下面这条区间仍然算、仍然用于排预算和推荐，只是不再各占主卡一行：
  **碳水区间不是照搬 AMDR 的 45%~65%，是拿当天热量和脂肪 AMDR 两端联立解出来的余数带。**
  搬 AMDR 会自相矛盾：高蛋白减脂档蛋白就占掉四成供能，照方案吃到的碳水（76g）
  远在 45% 供能（184g）以下，卡片会对着照方案吃的人写「低于建议 108g」。
  碳水真低到有生理风险时由 `carbBelowRda`（IOM 130g）说话，不归这条区间管。
  区间取整只许放宽（`floor` / `ceil`）：四舍五入过的下界曾比计划值本身还高 0.2g。
- **「当前消耗」是设备到此刻的静息 + 活动，直接相加，不外推**
  （`derived.liveEnergy.burnedNow`）。别拿 `dynamic.tdee` 当这个数 ——
  那份是按已过时长外推出来的**全天**值，早上八点就报出一千七，
  主卡的环上那条弧看着像「今天已经烧掉八成」，而实际累计只有几百。
  外推那个数是「预计全天消耗」，归 `targets.tdee`。
- **目标摄入按近期节奏算，一天之内不变；今天实际收支单独一个数。**
  原先目标跟着当天 Apple 累计走，晚上手表一同步目标就跳，同一顿饭从「刚好」变成
  「超标」——用户什么都没做错，是脚下的尺子在动。两个数各回答各的问题（`derived.liveEnergy`）。
- **「这个钟点该吃到多少」不是一条匀速直线**（`core/eating-rhythm.js`）。
  `judgeStatus` 原先用 `(hour-6)/16`，等于假设人从早到晚均匀地吃 —— 没有一个人是
  这样的：按它算中午 12 点该吃到 37.5%，而三餐比例下一顿午饭吃完就该到 60%，
  同一个人会被这条直线判成「吃得快了」。两套口径，用户在设置的「计算与显示」里选
  （`profile.rhythmMode`）：**guideline** 按《中国居民膳食指南》的三餐供能比折成折线，
  **personal** 用近 14 天自己的记录算真实分布。personal 每天各自归一化再平均，
  否则一顿 6000 kcal 的火锅就能把整条曲线拽偏；有记录的天数不足 7 天自动退回
  guideline，并且**必须在界面上说出来**——三天记录算出来的「我平常」不是我平常。
  那半句话只有 `paceNote` 一处产出，界面别再拼第二份。
  措辞用「多了 / 少了」不用「快了 / 慢了」：后者听起来像在评价进食速度，
  而这个程序根本没有进餐时长数据（「吃得慢一些」当年就是这么混进来的）。
  晚上 9 点之后不再说「少了」——那时候催人补热量等于劝人睡前大吃一顿。
- **减和增的速率上限不是同一个数**（`MAX_LOSS_RATE_PCT` 1% / `MAX_GAIN_RATE_PCT` 0.5%，
  都在 `core/nutrition.js`）。1% 是减重期的瘦体重流失风险提示线；0.5% 是增肌期
  常用体重增长参考的上沿，更快可能提高脂肪增加比例，但短期水分也会影响体重。
  共用一个 1% 会允许 45kg 的人计划每周 +0.45kg（一个月长 4% 体重）。
  计划说明、填写提示、健康提示、图下解读四处必须从同一对常量取值，否则同一个应用
  对「多快算快」会给出两个答案。
- **这两个数是建议上沿，不是闸门。** 证据支持的是「超过这个速度，相关风险可能上升」，
  不是「0.517% 不安全必须拦下」——这两句话不该由同一个机制执行。
  越线的值照填照算，代价由 `rateGuidance`（表单和主卡共用同一个判断）说出来；
  真正拦下的只有 `ABSURD_RATE_PCT`（1.5% 体重/周，护栏）那一档，而且只在表单里拦，
  `dailyTargets` 仍然要能容忍——恢复备份和云端同步是绕过表单直接落库的。
  按 0.5% 硬截断的代价能量出来：58kg 的人填 +0.30 被悄悄改成 +0.29，差 11 kcal/天，
  远小于食物估算和 TDEE 的误差，界面却要为此说「你填的 0.3 过快」。
  **说「被限住了」时只许点名最后真正起作用的那一条**（`rateLimitedBy`）：
  原先那句「已按体重比例和每日热量调整上限改为…」点了两个机制，实测只有后者碰到了。
- 速率一律写成「计划体重 +0.3 kg/周」。程序能规划的只是体重变化速度，
  长的是肌肉还是别的判断不了；目标名（减脂/增肌）保留，那是用户想做的事。
- BMR 使用公式原值，不设 800 kcal 下限；每日热量目标下限是女 1200 / 男 1500 kcal，
  不与 BMR 取最大值。饮水参考固定为男 1700 / 女 1500 ml（只用在说明文字里，不当进度目标）。
- `sugar` 目标和饮食汇总表示 WHO 游离糖，不是总糖或狭义添加糖。
  菜肴按 `DISH_ADDED_SUGAR` 表逐道扣掉食材自带的糖（表里没有的按不加糖处理）；
  乳糖和整食内源糖用 `natsugar` 标记。**加新菜时要顺手在那张表里给个值**，
  漏了会被当成完全不加糖。
- 体脂率输入会切换到 Katch-McArdle，但家用 BIA 误差会传导到结果，界面不得宣称“更准”。

## 说明层（`infoTip`）

**只写用户拿来判断和行动的话**：这个数是什么、可信到什么程度、该怎么用。
不写程序怎么算出来的 —— 统计窗口、分母取哪个、配对日的定义、字段从哪个来源取、
折算过程。那些是维护这个库的人要的，不是照着吃饭的人要的。

判断标准：删掉这句，用户会不会做出不同的决定？不会就删。

- 「近 7 日速览」那张卡原先挂着四段说明，全在讲窗口和分母 —— 整个删了，
  覆盖哪七天由右边的日期标签回答。
- **一件事一句话说完，别分两段。** 设置里的进食节奏原先是「主卡上『热量完成 45%』
  拿来和什么比」加上当前口径的说明：前一句讲这个开关是干嘛的、后一句讲选中那档
  是怎么回事，读的人得把两句拼起来才知道现在到底按什么算。
- 估算菜品原先每一道后面都跟一遍「原料比例、实际份量、用油…都会造成误差」，
  列五道就抄五遍；共有的那句由整张卡说一次（`estimateDisclosure` 把
  `note`（这道菜特有的）和 `generic`（这类菜共有的）分开返回，别再黏成一句）。
- **例外：「这个数现在不可信」仍然要说**，而且不许收进说明层
  （身体信息不合格 / 演示数据 / 数据过期 / 今天还没同步）。那是判断依据本身。
- **例外：数据不够时要说清还差什么才能给结论**（「至少 4 次称重、隔开 7 天」），
  那是用户能动手补的事。

**记号要有外圈**：一个点加一道竖线在正文旁边只是一处杂线，认不出这是个
可以点的入口。整体仍然是淡的（描边不填充、`opacity: .75`），15px 见方，
描边 .85。**上面那一点只填不描**（`.info-tip-dot { stroke: none }`）——
跟着 `.info-tip-mark` 上那道统一描边走的话，它的实际半径是 r 加描边的一半，
看着比下面那一竖粗出一圈，像个小球而不是一个点。

圆圈里的 SVG 小写 `i` 用原生 `<details>`，但原生只认 summary 上的点击 ——
打开之后用户以为随便点一下别处就能关掉，结果它一直挂在那儿。
`utils.js` 里装了一个 document 级的 `click` + `Esc` 监听补上这件事：

- 用 `click` 不用 `pointerdown`：后者早于原生的 summary 切换，点信息按钮本身会
  变成「先被关掉、再被原生打开」，闪一下。
- 靠 `closest('details.info-tip')` 判断点在不在自己里面 —— 点说明层内部
  （选中文字、点链接）不该关掉它。
- **监听器只装一次**（`infoTipDismissBound`）。每建一个 infoTip 就装一个的话，
  饮食记录里几十条记录就是几十个 document 级监听器。

## 弹层

底部弹层统一走 `lib/sheet.js`，别在各自的视图里再搭一个。有两处不好写对：

- **真正在滚的是 `<main class="view">`，不是 body。** 只给 body 加 `overflow: hidden`
  拦不住内容区自己的滚动容器 —— 表现就是「弹层里选不中，反而滑到了背后的页面」。
  打开时要把 main.view 也锁住，`.sheet` 还要 `overscroll-behavior: contain`。
- **关掉后要滚回原处，而且得等 onClose 里的重绘跑完再还。** 关弹层常常伴随一次
  内容重绘（清掉选中的食物、重插推荐卡），那次重绘会把 scrollTop 归零。

## 视觉规范

界面按 Apple 的路子走，核心是**有一套比例，而不是每处随手取一个数**。
改样式前先看 `css/app.css` 顶部的 token：

**只有一张样式表（`css/app.css`）和一份图标（`js/lib/icons.js`）。**
曾经还有 `css/ux-polish.css` 和 `js/ux-polish.js` 两层外挂精修：前者靠加载顺序
压住 app.css 的同名规则（实测 28 个选择器两边都写了），后者用一个 MutationObserver
在每次重绘后改 DOM。代价是同一个元素的字号和描边分散在两个文件里，
「改样式前先看 app.css 顶部的 token」这句话根本不成立。**别再新开一层。**

- **字号一律写 `var(--fs-*)`，不写 px。** 七档，**每一档都要看得出区别**：
  12 / 13 / 14 / 15 / 17 / 20 / 26。重构前光字号就有 23 种；收进 token 之后
  又漏回去过 214 处，其中 86 处小于 12px。`--fs-caption` 12px 就是可读下限。
  **最底下那三档曾是 12 / 12.5 / 13** —— 肉眼分不出，却占掉全应用 68% 的文字
  （caption 102 处、footnote 57 处、small 18 处）。三个名字做同一件事，
  「层级分明」就落不到实处：分不出大小的层级不是层级。`--fs-small` 已并进
  `--fs-footnote`，别再加回来。圆角四档（`--r-xs` ~ `--r-lg`）同样从这几档里挑。
- **控件高度、图标尺寸也是阶梯，不许写裸 px。**
  控件 `--control-sm` 36 / `--control-inline` 40 / `--control-md` 44 / `--control-lg` 48；
  图标 `--icon-xs` 14 / `--icon-sm` 16 / `--icon-md` 20 / `--icon-lg` 22。
  `--control-inline` 是行内那个圆形「把这一行加进来」，饮食页和健身页共用同一个数
  —— 同一件事就该一样大，各写一遍必然会漂。
- **按钮上的字只有三档**：主按钮和次按钮都用 `--fs-value`（它们常常并排站，
  次按钮小一档一眼就看得出没对齐）、普通按钮 `--fs-body`、胶囊和筛选 `--fs-footnote`。
  **按钮上的字不许压到 12px** —— 那一档是给单位和角标的，不是给按下去会发生事情的东西。
- **同一个选择器不许把同一个属性写两遍。** `.chip-btn` 的 min-height 曾经先写 32、
  文件末尾又写 36；`.primary-btn` 先写 44、末尾又写 `--control-lg`；
  `.settings-close svg` 20px 一遍 18px 一遍。看代码的人得先算层叠顺序才知道真值。
  `.nav-arrow` / `.icon-btn` 那几个更糟：写着 `width: 36px` 却被同一份 CSS 里的
  `min-width: 44px` 压掉，那几行是**死代码**，只会让下一个人以为自己调得动。
  （`@media` 里的覆写不算，那是正当的第二次声明。）
- **两层颜色别混着用。** 语义层（accent / warn / danger / 灰）回答「好不好」，
  数据层（`--protein` 紫 / `--carb` 橙黄 / `--water` 蓝）回答「这是哪一项」，
  **只在图表和营养条上用** —— 那儿有好几条线要分开，非用颜色不可。
  混起来的代价是具体的：主卡那排四个方框里另外三个的颜色说的是「到了 / 超了」，
  饮水却涂着数据蓝，一排四个三个说 A 语言一个说 B 语言；「纤维不足」那条提示
  左边的杠用碳水的橙黄，夹在绿和红中间就像第三种严重程度。
- **图标是画出来的，不是打出来的，而且只有一份。** × ＋ − ✓ › 这些排版字符
  在三个平台上是三种字形、三种基线，粗细跟着字重走，和描边图标凑不到一块儿
  （顶栏那个 ↩ 先踩的坑）。形全在 `js/lib/icons.js`，描边参数只在 `.icon` 上写一次
  —— 曾经 1.6 / 1.7 / 1.8 / 1.9 / 2.1 五个值散在五段 CSS 和三个 `createElementNS` 里。
  同一个形不许有两份：app.js 的前后箭头和 settings.js 的返回/展开箭头曾是
  逐字节相同的两串路径；体脂和饮水曾共用一个水滴，22px 上就是同一个图案。
  **一个形也不许读出两件事**：底栏「饮食」曾经用的就是满屏「加一笔」那个 `plus`，
  于是同一个加号既是「去饮食页」又是「把这一样加进来」；现在是叉 + 勺（`diet`）。
  秒表和钟当年分开是同一个理由。
  中间试过刀叉，那把刀在 22px 上是个带钩的形，看着像个「9」——
  **描边图标里凡是「一条线拐回来贴住自己」的形状，小尺寸下都会糊成一团**；
  勺子是个封闭的椭圆，再小也认得出，和叉齿的竖线重量也配得上。
  `test/ui.test.js` 里那条「图标是画出来的」会一起拦住这三件事。
- 卡片是「灰底 + 纯色卡」，`--shadow: none`。层次靠底色对比，不靠投影和描边。
- **不是这一屏主要动作的入口不染主色。** 饮食页的「＋ 自定义」一天用不到一次，
  染成绿色之后比下面那个搜索框还抢眼 —— 而搜索才是这张卡要人做的事。
- **强调色一次只用在一个地方。** `.secondary-btn` 那种 tinted 按钮是留给
  「这一屏的主要动作」的；四个并排全染绿就成了一整块色斑（喝水那排踩过）。
  **一排里只许有一个染色的**：和实心主按钮同处一行的次按钮退回中性灰
  （份量弹层底下的「继续添加 | 记录到午餐」）。
- **胶囊留给「可以选的状态」。** 点不动的说明写成一行文字，用「·」隔开
  （动作行下面那三条：模式 / 主练哪儿 / 复合还是孤立）。做成灰底小块之后
  一屏十几个色块比动作名还抢眼，筛到「胸」时五行的标签还一模一样。
- **同一排里不许有两种精度。** 主卡那四个方框（纤维 / 钠 / 游离糖 / 饮水）
  一律取整，原先只有游离糖带一位小数。上限类的取整用**向上**
  （`metricState` 的 `roundUp`）：18.4g 报成 18g 是把已经吃进去的糖说少了。
- **数字和单位之间空不空格由 `core/units.js` 定，别在拼字符串的地方自己决定。**
  西文单位前留一个空格（`116 kcal`），中文单位和 g / ml / kg / mg / % 不留
  （`30分钟`、`150g`）。之前同一页上能数出四五种写法。
- **互斥选择要有读屏语义**（`lib/ui.js` 的 `segmentedGroupProps` / `segmentedItemProps`）：
  切换「看哪一组内容」用 tablist / tab / aria-selected，选一个值用 radiogroup / radio /
  aria-checked，可叠加的过滤器（器械档位）才继续用 aria-pressed。
  只写 `aria-pressed` 的话，读屏听到的是「五个各自独立的切换按钮」。
- 互斥的选择一律用分段控件 `.range-switch`：灰槽 + 选中格浮起一块白。
  **「全部动作 / 推荐组合」也是**：它曾经是下划线 tab，而同一屏上另外两排是灰槽白格 ——
  三组做同一件事的开关摆出两套视觉语言。
  格子按内容分宽（`flex: 1 1 auto`）—— 等宽会让「固定器械 10」在 320px 的机子上被截断。
  **摞在一起的两三排之间要留缝**（`.range-switch + .range-switch`）：
  它们各自 `margin: 0`，直接贴着就是一整块被割开的灰色，分不出是两组不同的选择。
  一排实心药丸太响 —— 那只是「我现在看哪一组」，属于导航，不该比内容还抢眼。
- 底栏选中态只染色，不给图标垫色块，**也不加粗描边** —— 描边粗细统一这条规矩
  恰恰是从底栏定下来的，选中态再改成 2.1 就等于自己破自己的例。
- 「你现在看到的数字不对」这几条（`.data-freshness.warn`）必须一眼认得出是警告。
  这段样式在一次重构里被连带删掉过：文案还在、颜色和正文一样，单元测试全绿 ——
  `scripts/smoke.mjs` 里那条检查专门拦它。

## 容易踩的坑

- **缺数据的指标画一道杠（`—`），不要整格消失。** 格子凭空少一个，
  排布每天都不一样，而且「今天没测到」和「这个应用不显示这项」分不出来。
  体脂是例外（多数人没有体脂秤，常年挂杠只是噪音）；整张卡一个数都没有时
  走空状态，那不是「没测到」是「还没同步过」。
- **底部安全区只能算一次。** `.tabbar` 已经把它吃掉了；长在滚动容器里的东西
  （多选条）再加一遍，真机上就是凭空多出 34px 空白顶在按钮下面。
  只有盖住整个视口的 `.sheet` 才需要它，而且要走 `var(--safe-bottom)` 不是裸 `env()`
  —— iOS 独立运行时首帧 `env()` 常常还是 0。
- **`hidden` 属性会被元素自己的 `display` 压掉。** 只要写了 `display: flex/grid/block`，
  `hidden` 就不生效 —— 搜索框那个清除键就是这么变成常显的：JS 明明写了
  `hidden: !value`，而 `.search-clear { display: grid }` 让它一直站在那儿。
  CSS 顶部有一条全局的 `[hidden] { display: none !important; }`，别再给单个组件
  各补一条 `.xxx[hidden] { display: none }`（补到第十一处仍然漏掉了第十二个）。
- **给还没挂进 `<select>` 的 `<option>` 设 `selected` 会被打回第一项。**
  浏览器在插入时按 selectedIndex 重算。记录明明已经移到晚餐，下拉里还写着早餐
  就是这么来的：节点建好之后再设一次 `select.value`。
- **时长一律写成「6小时42分」**（`core/duration.js` 的 `formatDuration`），不写「6.7 小时」。
  小数小时只在图表纵轴上说得通（轴要一排等距刻度）；摆到卡片上就得让人
  把 0.7 乘回 60。AASM / SRS 共识按“成人规律睡够至少 7 小时”表述，不扩写成
  `7~9 小时`；设备时长不能替代睡眠质量和白天困倦判断。
  换成这个写法之后健康数据四列排布会被撑破（78px 的格子装不下 82px 的字），
  所以 `.metric-grid.cols-4` 单独降一档字号 —— `scripts/smoke.mjs` 里那条
  「1~8 项都排得平整」当场就拦下来了。
- **日期的纯函数在 `core/day.js`，`lib/utils.js` 只是再导出一遍。**
  「顶栏这行字该怎么写」是判断，得能写测试。改措辞改 `dayHeading`，别在视图里拼。
  措辞里只放文字：那个返回箭头由 `RETURN_ICON` 画成描边图标。
  打出来的 ↩ 在三个平台上是三种字形、三种基线，和旁边的中文对不齐，
  粗细也没法和底栏、健康数据那几个图标统一。
- **设置面板每次落库都会整个重建，`<details open>` 是 DOM 状态，重建一次就没了。**
  `app.js` 的 `subscribe` 和 `subscribeAccount` 都会调 `renderSettings`，
  于是「点一下同步，刚展开的那几节自己收了回去」——而同步恰恰是最会触发落库的操作。
  `data-manager.js` 里的折叠块一律走 `rememberedDetails(key, ...)`，
  **键要稳定**：拿标题当键的话，「手动补录 · 2026-08-29」翻一天就换一个键。
- **一次操作只弹一次提示，撤销由做这件事的那段代码自己给。**
  记录一笔食物曾经弹两次：`recordOne` 先弹一条不带撤销的，一帧之后
  一个全局 click 监听（按钮文字以「记录到」开头就算数）再覆盖一条带撤销的。
  那个监听还要 rAF 轮询 `state.dietEntries` 最多 1800ms 去找刚写进去的记录 ——
  落库慢过 1800ms 就彻底没有撤销。而 `addEntry` 本来就把新记录返回了
  （`runLocalAction` 原样放在 `value` 里），根本不用去 DOM 和 store 里找。
- **崩溃卡必须有出口**（`app.js` 的 `renderCurrentSafely`）。原先它只有标题和一句
  英文报错，而人正卡在打不开的那一页上。饮食页崩过一次，而它恰好是唯一能删掉
  那条坏记录的地方，于是彻底出不去。现在给「回到今日」和「复制诊断信息」，
  诊断走 `buildDiagnostics`（白名单式，只报条数不报数值）。
- **`closeSheet()` 会同步调用 `onClose`。** 弹层里「确认」按钮要先 resolve 再关：
  写成 `closeSheet(); finish(value)` 的话，`onClose` 里的 `finish(null)` 先落地，
  用户按了「复制」拿到的却是「取消」—— 弹层关掉了，什么都没发生，也没有提示。
- **别把已经算出来的数字从渲染结果里读回去。** 份量弹层那根碳水/脂肪刻度
  一度是靠 `.impact-to` 的文本正则出两个数再算一遍的，而 `n` 和 `gaps` 就在手边。
  回读一旦碰上改文案、换单位、数字带千分位就会静默失效，
  界面上只表现为「那一行忽然变回两行」。
- **`Number(null)` 是 0，而 `Number.isFinite(0)` 是 true。**
  `lineChart` 曾经用 `Number.isFinite(Number(d.y))` 直接过滤，漏记的那天就这么
  混成「吃了 0 kcal」的实点，把折线拽到地板上——而图下面那段解读用的是
  `analyzeSeries`（它剔了 null），同一张卡里两句话互相打脸。
  凡是「先转数字再判断」的地方都要先剔 `null`。
  `trend-reading.js` 里的「有几天低于 X」也踩过同一个坑：`analyzeSeries` 剔了 null，
  旁边六处 `points.filter((p) => Number(p.y) < X)` 没剔 —— 14 天里只有 3 天有记录、
  都贴着目标，卡片却写「另有 11 天不到目标的四分之三」「11 天不足 6.5 小时」。
  现在统一走 `countDays()`，分母分子用同一批天。
  `health-insights.js` 的 `hasHealthMeasurement` 也踩过：一条「字段全是 null 的空壳」
  被当成有测量，窗口终点被拉到那天，近 7 日体重从 6 条实测掉成 1 条；
  `health-metrics.js` 判断体脂、饮水这类 optIn 指标该不该占一格时同理 ——
  一个空值字段会让它们常年挂着一道杠。
- **能量单位区分大小写**：Apple 导出的 `unit="Cal"` 是大卡（kcal），小写 `cal` 才是 1/1000。
  先 `toLowerCase()` 再比较会让整套数据缩小一千倍 —— 这个 bug 上线过，`findMisscaledEnergyDays` /
  `repairMisscaledEnergy` 就是给用户修历史数据的。
- **HealthKit 的 `%` 存的是 0~1**（0.181 表示 18.1%），第三方导出却直接写 18.1，
  `normalizeValue('percent')` 按数值大小区分。
- `addEntry` 的字段叫 `sugarLevel` 不是 `sugar`：后者是展开的 nutrients 里糖的克数，会互相覆盖。
- 建 DOM 用 `utils.js` 的 `h()` / `mount()`，别用原生 `append()` —— 原生不展开数组
  （得到 `[object HTMLButtonElement]`），也不忽略 `null`/`false`（渲染出字面量 "null"）。
- **CSS 自定义属性只能用 `setProperty` 写**（`h()` 里的 `applyStyle` 已经代劳）。
  `Object.assign(el.style, { '--foo': '4' })` 不报错也不生效，排版看着「没变」而已 ——
  健康数据的列数就是这么静默失灵的，`scripts/smoke.mjs` 里那条格子排布检查专门拦它。
- `lineChart` 的纵轴刻度逻辑在 `test/charts-axis.test.js` 里抄了一份（charts.js 依赖 DOM）。改一处要改两处。
  调用方传的 `decimals` 只是**下限**：量程比它还细时（一周体重只差 0.08kg）
  四条刻度会印成「62.0 / 62.0 / 61.9 / 61.9」，所以撞车了会自动往上加位。
- 图上的虚线要在标签里写清是「建议 / 目标 / 平均」中的哪一个。
  只写「7 小时」，旁边卡片角上又写着「平均 6.6 小时」，两个数对不上，看的人只会以为算错了。
- 新增 js 模块要加进 `sw.js` 的 `SHELL`，并同步 `package.json` / `js/core/feedback.js` 里的版本号
  （`test/ui.test.js` 和 `test/feedback.test.js` 会一起卡）。漏一个模块，离线时整个应用就打不开。
- 跨模块调用忘了 import 只会在浏览器里炸（设置抽屉整页白屏就是这么来的），
  `test/module-refs.test.js` 是手搓的 `no-undef`，替代不了 eslint 但能拦住这一类。
- 中文默认允许在任意两个字之间断行：短标签（`游离糖上限`、`1g 蛋白`）会被断成两截，
  要么 `white-space: nowrap`，要么 `word-break: keep-all`。
- **焦点在输入框里时不许整页重绘**（`app.js` 的 `renderCurrentSafely`）。
  定时器、可见性、账号轮询这几条路一直都记得躲开，唯独 store 订阅那条漏了 ——
  在饮食记录里改克数时，后台任何一次落库（五分钟一次的账号健康轮询、云端同步）
  都会把正在编辑的 input 连根换掉：焦点回到 body，iOS 收起键盘，敲了一半的数字也没了。
  跳过之后要记一笔，`focusout` 再补一次重绘（并且要等一拍再判断焦点，
  否则在两个格子之间跳会把人从第二个框里踢出去）。
  账号隐私锁是例外，必须 `force: true`。
- **`recompute()` 里不许把异常抛出去。** 它在 boot 的 `hydrateStore()` 里就会跑一次，
  抛出去 = 整个应用起不来，而且用户连设置抽屉都打不开、没法回去改那条数据。
  身体信息算不出目标时退回默认档案，把原因记进 `derived.profileError` 让界面去说。
  （`saveProfile` 有校验，但恢复备份和云端同步是绕过它直接落库的。）
- 启动失败要调 `__HEALTH_DIET_BOOT__.fail(err)`，**不要先 `clearEl(viewRoot)`**：
  启动页在 `#view` 里，摘掉之后 `showRecovery()` 会因为 `isConnected` 为 false
  直接返回，「修复缓存并重新打开」那个按钮就永远不出现了。
- 份量、克重这类会直接显示的数，取 min/max 时要留意另一侧是不是浮点——
  推荐里出现过 `海鲜粥 384.00000000000006g`，来源是「剩余热量 ÷ 每 100g 热量」。
- **身高、体重、体脂只有 Apple 健康一个来源**，档案里那份手填值只在设备从来没给过
  这一项时才顶上（新用户、清空重来）。界面上对应地把这三个字段变成只读，
  因为「摆着能改的输入框、算的时候却用设备记录」比锁死更让人困惑。
  取值用 `latestHealthEntry`：称重不是每天都有，当天没有就沿用最近一次，
  并把那天的日期显示出来——别让前几天的数看起来像今天刚称的。

## 约定

界面文案、代码注释、commit message 一律中文。commit 用 `feat:` / `fix:` + 中文摘要。

注释写「为什么」而不是「是什么」，通常是记录踩过的坑（看 `app.js` 的 `syncOnboarding`、
`diet.js` 的文件头注释）。新写的注释照这个来。
